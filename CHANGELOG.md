# Changelog

All notable changes to the crateon-bundle skills are documented here, one
line per skill per release, so an updater can decide replace-vs-merge
without diffing. The format is based on [Keep a Changelog](https://keepachangelog.com/).

## [Unreleased]

### Added
- **`docs/presentations/crateon-bundle-skills-map.html`** — second runtime-map artifact: all 20 published skills as blocks (the skill's "blocks = services, subsystems, or skills" rule exercised on the skills taxonomy). Four lifecycle lanes (вход/планирование → исполнение/discovery → закрытие эпика → коммиты/upstream), 17 causal edges, 6 scenarios (Вся система default · инициация · кампания op-watch · закрытие эпика · коммит-петля · upstream). First map with **full catalog parity**: every node carries `data-service` checked against the README skills table — which step 2 ("code/manifests authoritative, fix the catalog doc in the same change") forced honest: the table said 16 skills and was missing op-audit/op-debug/op-watch/runtime-map-updater; now 20/20, both parity directions negative-tested. Open-task slice is honestly empty (all bundle epics closed; the dialog says the shared EVAL-7 debt lives on the toolchain map).
- `runtime-map-updater` — **whole-system scenario is now part of the contract** (matched to the origin artifact after user review): the scenario bar MUST open with a first "Вся система" button (`data-scenario="all"`, `aria-pressed="true"` on load — every node and edge lit, nothing dimmed; re-clicking an active route returns to whole-system, never to an all-dimmed board) followed by the all-tasks button second. Encoded three ways: a new validator regex (`whole-system scenario selected by default` — the cognitron original passes it too), the design-language scenario-bar section, and SKILL.md step 5. Step 3 now also states the refresh rule explicitly: the open-task slice is recomputed from the op-*-maintained epics on every HTML update (live debt, not a remembered snapshot), and blocks are whatever the project declares as operating units — services, subsystems, or skills. The bundle map rebuilt accordingly; both maps green under the extended contract.
- `runtime-map-updater` — **`references/design-language.md`**: the map's LOOK is now a stated contract, not folklore. The first fresh-map run proved the F-V lesson on visuals: the origin artifact's polish (graph-paper surface, tape strips, tilted hand-drawn nodes with marker stripes, edge shadow-twins with halo labels, layer labels, scenario notes, panel anatomy — kicker/marker headings/hand-drawn step counters/3-col boundary grid/priority pills, filled-gold epic chips, draw-in stagger) lived only in cognitron's hand-refined HTML, so a from-scratch build satisfied the 14 validator regexes yet read as a wireframe. The design language is extracted into the reference (tokens, hand-drawn primitives, per-component anatomy, motion + reduced-motion, mobile), SKILL.md step 6 now read-triggers it for new maps/restyles and forbids regressing an existing map to the mechanical minimum. `crateon-bundle-runtime-map.html` rebuilt to the full language as proof — its own validator green (12 nodes, EVAL-7 the single open chip).
- **`runtime-map-updater` — new published skill (20th)**, ported and generalized from cognitron's `.agents/skills/runtime-map-updater`: maintain an interactive HTML runtime/architecture map as a *verified* artifact (nodes backed by docs/manifests, task chips only from open units, interaction contract machine-checked). Generalizations vs the origin: sources resolved from the project's declared layout (AGENTS.md pointers / spec-systems / BMAD or flat epics) instead of hardcoded cognitron doc names; validator ported python→node (`scripts/validate-runtime-map.mjs`) with optional `--catalog`/`--tasks-dir` (absent sources are skipped with a note, not failed) and generalized task-ref grammar (`Task N` and `UNIT-ID` styles, RU+EN closed tokens); e2e-spec step made conditional; bundle-standard Safety Contract + canonical Completion Status added. Port verified by convergence on the original cognitron artifact (15 services · 14 nodes · 10 task refs — OK, same as the python validator); teeth verified negatively (closed-task ref and node/nodeDetails mismatch both error). First artifact: `docs/presentations/crateon-bundle-runtime-map.html` (12 nodes, toolchain flow authoring→validate→eval→dist→host→op-cycle with the friction return loop; the single open chip is EVAL-7). Trigger corpus 74/74·60/60 with max cross-collision 0.142; ergonomics smoke 11/11; dist + both hosts byte-identical.
- `op-feedback-harvest` — **`scripts/validate-feedback.mjs`**, the mechanical form of the apply-mode validation the skill previously promised only as prose (was "declined, nice-to-have" in W2-1 — shipped by the backlog-closeout campaign): dead `index.md` routes, orphan rule files, missing `Checklist (for preflight)` sections, dead pointer-form `doc#anchor` targets (the W2-1 lesson made checkable), audit-checkpoint 5-section shape, and secret patterns (locations reported, values never echoed). Smoke `scripts/eval/validate-feedback-smoke.mjs` 8/8; ergonomics smoke extended to 10/10; SKILL.md step 9 cites the command. Synced to dist + host (claude/codex), byte-identical.
- `docs/tasks/skill-authoring-optimization.md` — **epic fully closed (11/11)**: SKILL-3 flipped to verified (kept half measured — 19/19 published skills carry ≥3 positive trigger scenarios in the eval corpus; third-person conversion stays deferred as a measured no-op per EVAL-4) and SKILL-5 flipped to verified (executed as EVAL-5's `run-behavioral-evals.mjs` — supersedes the acceptance line's `skill-forward-test.mjs` filename — 8 skills with the leaked-answer control, grader self-test 8/8).

### Changed
- **op-debug / op-docup / op-planner / op-decision-memory / op-feedback-harvest hardened from wave-2 e2e** (5 fresh-operator agents, all held write discipline; 5/5 greenable-not-green). op-planner: **target-existence check before proposing** (refused to mint a story against code a later story deleted — the load-bearing fix) + no-materialize-obsolete-on-apply + BMAD-default + stale-checkpoint note. op-docup: **Workflow step 0 "locate the sync baseline"** (the title's checkpoint was never operationalized) + re-derive-row-values-at-apply + index/table-only mapping option. op-debug: `DONE — already fixed` status path (the cognitron defect was already fail-closed at HEAD, independently verified) + static-confirmation-as-repro + scope-out-of-op-audit. op-decision-memory: ADR-vs-changelog contrast + one-ADR-per-decision granularity + rationale-from-story-first + seed ADR-001. op-feedback-harvest: explicit-commit-list window + pointer-form-needs-real-anchor + read-matched-rule-body-before-DUPLICATE + WATCH-rule-outlived-surface. Corpus 25/0/0; dist 19/0/0.
- op-* skills — **e2e-hardening backlog OSH-4..7 implemented** (docs/tasks/op-skill-e2e-hardening.md): op-audit gained a concrete Task-fallback recipe so `run` is executable without the Workflow tool (per-lane prompt + inline FINDINGS_SCHEMA fields + self-run adversarial verify) and a `--report-only`/`--dry-run` read-only stance (findings to conversation, nothing written); op-preflight lifted the ~400-word stack-deviation gate into `references/stack-gate.md` (step 3 now a trigger + read-pointer, thin router preserved); op-debt-scan now mandates a pre-emit arithmetic self-check on the priority-score line. validate:dist 19/0/0; source 25/0/0.
- **op-* read-only skills hardened from a real-repo e2e agent run** (`op-preflight`, `op-story-finder`, `op-tour`, `op-drift-check`, `op-debt-scan`; `scripts/lib/stack-check.mjs`). 6 parallel "fresh operator" agents each ran one skill end-to-end against a large real project (read-only) following its shipped SKILL.md literally; 5/6 came back **greenable-not-green** — the skill completed only because the agent improvised past its own written rules. Systemic root cause (4/5 skills): the skills hardcoded BMAD-default globs (`docs/**/stories/*.md`, `architecture.md`/`prd.md`) and never consulted the project's declared layout (`docs/spec-systems.md`), so a flat `docs/EPIC_*.md` backlog yielded false "no units"/"no docs"/BLOCKED. Fixes: detect the declared layout first + support flat-epic `### Task N — STATUS` units + a detection guard (0 hits next to EPIC_*.md = miss, not empty); exclude non-authoritative trees (`.claude/worktrees/`, `data/`, `agent_workspaces/`) from globs; resolve re-export shims (`sys.modules[__name__]=_impl`) before flagging code "missing" (killed a false-CRITICAL class in op-drift-check); status/priority parsing (priority tag ≠ status; done→CURRENT/PLANNED→TARGET so PLANNED isn't a false WARNING). Real defects also fixed: **op-preflight** pointed at a non-existent per-project `scripts/lib/stack-check.mjs` (MODULE_NOT_FOUND for any fresh operator) — now points at the global-tree `~/.claude/scripts/lib/stack-check.mjs` with the resolution anchor, and `stack-check.mjs --help/-h` now prints usage instead of no-op'ing; **op-debt-scan** promised outdated-dependency detection its `allowed-tools` can't run — downgraded to lockfile-read, plus a scoring rubric (unreproducible 1–5 axes now anchored), a tests/**-wide test-lookup order, and a suspicious-artifacts pass (which surfaced a real tracked secret-dump in the target). Friction matrix: `docs/research/op-skill-e2e-friction-cognitron.md`. Source 25 + dist 19 skills: 0 errors / 0 warnings.
- `upstream-harvest` — **deterministic final message** (F-V pattern): the end-of-run reply varied per model/run (tested on glm-5.2) because only the checkpoint format and completion token were specified — the final report was not. Added a non-negotiable `## Final message` block (verbatim field labels, ABSOLUTE paths): `Repository:` (from `git remote get-url origin`), `Upstream code:` (…/upstreams/<PROJECT>), `Upstream docs:` (…/upstreams_docs/<PROJECT>), `Range: … · Epics · Stories · Tests`; checkpoint-only runs suffix the docs path with `(not written — no --apply)`. Verified on glm-5.2 (a local crawl4AI harvest): two independent runs emitted byte-identical blocks.
- `docs/SKILL_STANDARD.md` + `scripts/validate-skills.js` + `project-doc-kit` — **SKILL-0/7/1 of the skill-authoring epic implemented** (baseline → policy → validator hub). SKILL-0: baseline appendix in the epic (19 published vs 6 repo-only scope pin; fresh sizes — desc max 503 ch/74 w, body max 207 lines, all inside every cap; ONE bare `<>` offender, not two — op-watch's `<intent>` was already backticked). SKILL-7: new SKILL_STANDARD section "Content placement & budgets" — the normative source the validator cites: `Use when` second-person kept (third-person deferred), ≥3 enumerated triggers defined, hard caps (desc ≤1024, name regex, code-span-scoped `<>` ban), size-pressure warning tier (~75–100-word target, warn >120), local-vs-dynamic placement table, ≤30-line inline-code rule, no-duplication invariant, read-trigger rule for reference links, clutter blocklist with `skill-payload: required` escape hatch, and the F-V stated-contract lesson. SKILL-1: validator encodes it — errors (bare-`<>`-outside-code-spans, clutter files) + warnings (>120 words, <3 enumerated triggers, naked reference link = <5 words of surrounding prose) — messages cite the standard; `project-doc-kit`'s `<YYYYMMDD>` backtick-wrapped. New `scripts/eval/validate-skills-fixtures-smoke.mjs` (9/9): every new rule fires on a violating fixture, escape hatch + backticked-placeholder + annotated-link controls stay clean. Source 25 skills and dist 19 both 0 errors/0 warnings.
- `docs/tasks/skill-authoring-optimization.md` — **epic revised per the four-reviewer grilling** (Claude/kimi/GLM-5.2/gpt-5.5; feedback in `docs/research/skill-authoring-epic-grill-feedback.md`). v1 was not build-ready: the `<>`-in-description ban would have failed two shipped skills (op-watch `<intent>`, project-doc-kit `<YYYYMMDD>`), third-person fought the existing `Use when` validator, the SKILL-7 ordering was circular, four units were partially done, and the budgets blessed the status quo. Revision: stable IDs kept; **+SKILL-0** (baseline audit + scope pin: hard rules → the 19 published skills, repo-only 6 → warnings) and **+SKILL-10** (completion-status byte-identity snapshot guard — the invariant was invoked everywhere but protected nowhere); **SKILL-8 dropped** (zero clutter files — its guard folded into SKILL-1 with an escape hatch); **SKILL-3's third-person half deferred** (kept: ≥3 enumerated triggers + before/after measurement); SKILL-1 re-scoped as the validator hub encoding SKILL-7's decisions (code-span-scoped `<>` ban, size-pressure warning tier, absorbed link-trigger + clutter lints, negative fixtures); SKILL-5 re-scoped to a gated spike on the existing harnesses with a leaked-answer control; SKILL-6 to TOC-only for the 5 references actually >100 lines. Ordering fixed: 0→7→1→content→5. Cross-cutting: audit-then-delta, the four-artifact separation (policy/rule/fixture/migration), and the F-V "state every hard rule" lesson.
- `op-watch` (`init.mjs` + SKILL/protocol) + eval harness — **cross-model quality pass (F-V): charter schema aligned + severity-gate rule made explicit; 20/20 contract scenarios on 4 models.** A statistics run on the test project (kimi-k2.7, glm-5.2, deepseek-v4-flash, mimo-v2.5-pro; crt executed only in the test folder) scored 17/20 contract scenarios + 4/4 valid real derives. The three failures were all the same scenario — the Critical-unit severity gate — and root-caused to the harness contract never *stating* the rule (models had to guess the exact `deferred` status; safety held in every case — no model auto-fixed). Stating the rule in the contract → re-run **15/15**, final **20/20 across all four models**. Also fixed the surfaced schema gap: SKILL/protocol prose described the charter as an object while `init.mjs` writes `charter` as the intent string + top-level `done_condition`/`campaign_type` (where scripts actually read them) — prose now matches the script, and `init.mjs` gained `--test-language/--anchor/--branch` flags so the F-N/F-P test-language freeze is persistable through the mandated script path. Notable regression win: mimo, which pre-F-T wrote an invalid `pending` status and stalled, now derives a perfect 10-unit gated state. Stats: `T:/project/crateon-dev/docs/.eval/op-skill-quality-matrix.md`. Smokes 8/8·22/22·23/23·9/9; `validate:dist` 19/0/0.
- **Skill context-footprint optimization** (progressive disclosure, grill-pattern): the op-* SKILL.md files were context-heavy. (1) **op-watch split into a router + `protocol.md`** — the router (`SKILL.md`) keeps every command + the step sequence/dispatch table/outcomes (**211→113 lines**); the 8 invariants, resumability contract, and gate/test-language rationale move to `protocol.md` (71 lines), read on demand (the deterministic scripts enforce the hard parts regardless). **op-audit** split the same way (**252→207** + `references/operations.md` 78 lines): the rare `tasks` beads/planner/file backend recipe, `run --spec` detail, and stance rationale move to the reference; the router keeps run/verify/plan/status + the command/flag tables. **op-docup** (**199→163**): the Sweep-mode deep mechanics (already duplicated in `references/sweep.md`) collapse to a thin router keeping only the segmenter + Tier-1 Workflow invocations. Re-verified on the test project: kimi ran op-audit guided-help correctly from the slimmed router and followed the pointer to `references/operations.md` for the export recipe. (2) **Verbose descriptions trimmed** to a tight trigger+purpose (op-audit 803→469, op-watch 570→379, op-docup 539→430, op-feedback-harvest 528→431) — descriptions only drive *selection*, so the mechanism essays moved into the bodies; the always-in-context op-* description index drops ~5.5KB→4.8KB with the validator's `Use when` trigger phrasing preserved. **Verified no quality loss across three models on the test project:** kimi + mimo (claude wire, real skill file) each derived the meta-server campaign correctly (9 units incl. gate-as-units, charter frozen, `verdict.mjs` → CONTINUE; mimo cited the gate-as-units invariant); glm-5.2 (OpenAI wire, router rules inline) executed the dispatch/verdict contract correctly. `validate:dist` 19/0/0; op-watch smokes 20/22/9/8 green.

### Added
- **SKILL-2/4/6/9 of the authoring epic done** (with SKILL-10 earlier, the epic's remaining units close). SKILL-4: op-* script agentic-ergonomics scan clean (no traceback leaks / uncapped dumps) + regression guard `script-ergonomics-smoke.mjs` 9/9. SKILL-9: `docs/reference/op-skill-tooling-fallbacks.md` (8 rows, soft/hard-classified) + `tooling-fallback-smoke.mjs` 2/2 exercising accept.mjs stack auto-detect. SKILL-6: TOCs on the 3 non-template refs >100 lines (sweep/methodology/conventions; templates exempt as generated). SKILL-2: naked-reference-link lint green corpus-wide, no exemptions.
- **W2-5: checkpoint-artifact section contracts** — op-feedback-harvest + op-decision-memory now state their checkpoint/report's required sections explicitly (op-planner/op-docup already did); the behavioral grader asserts the written checkpoint carries them (`scenario.sections`, confirmed on haiku: planner/harvest 4/4 incl. the section check). Wave-2 epic (op-debug/op-docup/op-planner/op-decision-memory/op-feedback-harvest) complete: W2-0..5 verified.
- **W2-4: cross-model wave for the 5 wave-2 skills** — `--crt` grader extended for checkpoint scenarios; 5 new scenarios × mimo/deepseek/kimi all GREEN (+ haiku direct from W2-3). Every initial RED triaged to a single harness limitation: crt print-mode blocks Write, and checkpoint skills emit their token only AFTER writing, so weaker models correctly stall pending the (blocked) write instead of fabricating DONE — the token is now informational under crt (graded hard in --run). read-only skills GREEN on every model. glm-5.2 skipped for the new 5 (reasoning-slow).
- **W2-3: behavioral fixtures for the 5 wave-2 skills** — `run-behavioral-evals.mjs` gains 5 scenarios (debug/decision read-only; docup/planner/harvest checkpoint) + a per-scenario write-discipline mode graded by fs-snapshot diff of the isolated temp workspace (the worktree-grader): read-only=zero writes, checkpoint=EXACTLY the checkpoint file. git history seeded in-temp for docup/harvest. `--grade-fixture` now 8 assertions (both modes, incl. checkpoint writing a 2nd/modifying file). Live haiku: debug/decision/planner/harvest 4/4·4/4·3/3·3/3 GREEN; docup write-discipline GREEN but completion-token flaky ~50% (haiku checkpoint-mode variance — its block is byte-canonical per SKILL-10; not over-fitted). 7/8 stable-GREEN.
- **SKILL-10: Completion-Status guard** in `validate-skills.js` — with a myth-busting correction: the 'byte-identical block' invariant was FALSE (all 19 blocks unique; the protocol sentence had drifted into 3 variants). The real contract is now guarded: canonical protocol sentence byte-identical + exactly the 4 tokens in order + section present. All 19 skills unified to one canonical sentence, which also lands the EVAL-5 plain-token hardening ('as plain text — no markdown emphasis or backticks' — the haiku `**DONE**` finding). Fixtures 14/14; both corpora 0/0.
- `docs/tasks/op-skill-wave2-hardening.md` — **epic: wave-2 hardening for the 5 op-* skills the first arc didn't touch** (op-debug, op-docup, op-planner, op-decision-memory, op-feedback-harvest — they got shared infrastructure but no e2e pass, no behavioral coverage, no write-path exercise; wave-1 base rate: 6/6 e2e'd skills came back greenable-not-green). W2-0 pins per-skill scenarios + write-discipline expectations FIRST (fs-diff-graded: default run = exactly the checkpoint file, --apply = exactly the declared artifacts); W2-1 read-only trio e2e (op-debug on cognitron vs a real audit finding); W2-2 write-pair e2e on crateon-dev; W2-3 fixes + 5 new behavioral fixtures (8 scenarios total); W2-4 cross-model wave (target 8×5=40 matrix cells); W2-5 checkpoint-artifact required-section contracts (the upstream-harvest lesson generalized). Plan only — no unit executed.
- EVAL-6 second wave — **deepseek-v4-flash 3/3 + kimi-k2.7 3/3 GREEN** (both vault keys live). **Final behavioral matrix: 5 models × 3 scenarios = 15/15** (haiku/mimo/glm/deepseek/kimi × story-finder/preflight/tour); the completion-token and no-writes-without-apply contracts held in every run. Epic skill-eval-adoption complete: EVAL-0..6 verified, EVAL-7 deferred.
- `run-behavioral-evals.mjs --crt KEY:model` — **EVAL-6 cross-model behavioral smoke, first wave (glm-5.2 + mimo-v2.5-pro): 6/6 GREEN.** crt manages --output-format (no trace), so the crt mode grades observables: completion token from the printed answer, **safety via workspace fs-snapshot diff** (output control), reads via content-evidence (ids/paths present only in fixtures). Workspaces under the sanctioned T:/crateon-dev-replay, cleaned after. mimo 3/3 (32–59s), glm 3/3 (83–204s); with haiku (EVAL-5) the matrix is 3 models × 3 scenarios = 9/9 — completion-token and no-writes held everywhere. kimi/deepseek = optional second wave.
- `scripts/eval/run-behavioral-evals.mjs` + `behavioral-fixtures/` — **EVAL-5 behavioral trace-grading spike done** (their Tier-3 design + our SKILL-5 leakage discipline): headless `claude -p` stream-json runs in throwaway workspaces (fixtures seeded from the crateon-dev test project), graded DETERMINISTICALLY over observed tool calls — incl. standing expectations for our differentiators (completion token last line; zero write-tools without --apply). **3/3 scenarios GREEN on haiku** (story-finder/preflight/tour), **leakage control holds** (planted ANSWER.md can't flip the grade — observed reads required), grader unit test runs at zero tokens (`--grade-fixture`). Yield: haiku bolds the completion token (`**DONE**`) — grader normalizes; recorded as a Completion-Status prose-hardening candidate gated on SKILL-10; run-to-run variance documented (per-run threshold; smoke, not certification); win32 invocation nuances (skill via workspace CLAUDE.md, prompt via stdin, EBUSY-safe cleanup, /dev/null-redirect not a write).
- `docs/research/eval4-third-person-experiment.md` + `docs/research/eval4-third-person-variants.json` — **EVAL-4 done (the deferred SKILL-3 third-person half, finally MEASURED)**: fresh-operator agent ran a single-variable A/B on 8 skills (both confusable pairs) — **perfect no-op** (30/30 positive ranks unchanged, 24/24 negatives clean, cosines identical to 6 decimals, matrix byte-identical; Porter-stemming makes person/voice invisible to the ranker). Third-person tied, didn't win → `TRIGGER_PHRASING` stays, conversion deferred as a prose-consistency option; real trigger gains require content changes. Tool friction fixed: `--help`, `--skills-root` A/B corpus-comparability warning + Git-Bash path note, `--cases` aggregate-only note. Comprehension green — the EVAL-2/3 drill fixes held.
- `validate-skills.js` usability from the EVAL-3 rename drill (fresh-operator agent, verdict GREEN — lint caught 100% of a rename's broken refs in one iteration): any `-*` arg now prints a usage string documenting the bare POSITIONAL <skills-dir> arg (was: `--help` treated as a path → misleading 'directory not found'), and SKILL_STANDARD 'Verifying a change' documents the temp-copy rename/refactor dry-run invocation.
- `validate-skills.js` — **EVAL-3: dead cross-reference lint** (adopted from agent-skills): any `op-*`/`upstream-*` token naming no skill in the validated tree → error citing SKILL_STANDARD's exact-published-name rule. Tree-relative known-set means a published skill referencing a repo-only skill fails `validate:dist` (published set stays self-contained). Precision guards earned on the first corpus run: derived artifact names (`op-watch-verdict-smoke`) and path segments (`upstream-proposals/` — a real directory in upstream-cherry that the naive rule flagged) are skipped. Fixtures 11/11; source 25 + dist 19 both 0/0.
- `trigger-rank.mjs` hardening from the EVAL-2 operational drill (2 fresh-operator agents: triage=green, collision=blocked-then-fixed): full usage string (documents `--allowlist` semantics, `--skills-root` as the contributor dry-run mechanism, the exit-code-behind-a-pipe trap that fooled both agents AND the lead), unknown flags exit 2 instead of silent swallow, a malformed allowlist entry no longer excuses its pair (errors AND shows no ALLOW), and `trigger-rank-smoke.mjs` (11/11) is wired into `eval:triggers` so the never-excusable ≥0.75 rule is CI-exercised. One drill claim was a false alarm (the pipe trap itself).
- `npm run eval:triggers` + `scripts/eval/trigger-collision-allowlist.json` — **EVAL-2 done: Tier-2 gate wired into CI.** `validate` and `validate:dist` now end with the collision gate (≥0.75 always fails, never allowlistable; the 0.5–0.75 warn band fails unless the pair has an explicit `{a, b, rationale}` allowlist entry — collision max cannot silently grow past baseline) + the 19-file trigger-case gate (70/70 positives, 57/57 negatives). Allowlist seeded with the one baseline pair (upstream-cherry↔upstream-harvest 0.639). Smoke extended to 11/11 incl. allowlist semantics (ALLOW/un-allowlisted-ERROR/never-excusable-ERROR-band/malformed-entry).
- `scripts/eval/trigger-cases/` (19 files) — **EVAL-1 done, authored by 3 parallel fresh-operator agents as a comprehension test**: per-skill positive/negative trigger cases; final corpus **70/70 positives in top-k · 57/57 negatives not-#1 · 0 errors** (`trigger-rank.mjs --cases --gate`). The only warn-collision pair (upstream-cherry↔upstream-harvest 0.639) held both directions with explicit boundary negatives; 0 EVAL-4 candidates (both round-1 failures were bad prompts, not trigger gaps). Comprehension verdict **greenable-not-green** → fixes: the case JSON schema is now pasted verbatim in the epic (was described-not-stated: all 3 agents burned 5–25 blind probes; F-V again), runner name corrected, `trigger-rank.mjs` now ERRORS on a zero-contribution case file instead of silent 0/0 green (smoke 6/6), README distinguishes the deterministic harness from the model-based one.
- `scripts/eval/trigger-rank.mjs` + `trigger-rank-smoke.mjs` — **EVAL-0 implemented**: faithful zero-deps port of the agent-skills Tier-2 lexical ranker (tokenize→stem→TF-IDF→cosine; name tokens 2×) over the 19 published descriptions, modes `--matrix`/`--rank`/`--cases` (+`--gate`); the EVAL-1 case-runner core shipped early. Fixture smoke 5/5 (collision-ERROR fires on a name+description-close pair, mis-owned negative fails, unknown owner errors). BASELINE recorded in the epic: 171 pairs, 0 error-collisions, 1 warn (upstream-cherry↔upstream-harvest 0.639, allowlist candidate); sanity probes all rank #1 correctly.
- `docs/tasks/skill-eval-adoption.md` — **epic: adopt the agent-skills measurement mechanics** (from the upstream comparison: they win measurement, we win execution). EVAL-0..7 with a fixed order (measure first, change second): port the zero-deps trigger ranker (tokenize→stem→TF-IDF→cosine) + record the 19×19 collision/probe BASELINE; per-skill trigger cases (positives top-k + negatives-with-owner encoding routing boundaries like op-audit vs op-debt-scan); collision gate in CI (warn 0.5 / error 0.75 + documented allowlist); dead cross-reference lint; description experiments A/B'd against the harness (re-opens SKILL-3's deferred third-person half WITH a metric); behavioral trace-grading spike merging their Tier-3 design (stream-json trace, grade tool calls not prose, --dry-run) with SKILL-5's leakage control PLUS standing expectations for our differentiators (completion-token last line, no-write-without---apply); cross-model behavioral smoke via crt. Includes a measurement calendar (what/when to re-measure) and the no-prose-changes-before-EVAL-1 rule. SKILL-3/SKILL-5 in skill-authoring-optimization.md now route to EVAL-4/EVAL-5.
- `docs/research/upstream-skill-authoring-findings.md` + `docs/tasks/skill-authoring-optimization.md` — cross-ecosystem skill-authoring survey (Anthropic/Microsoft/Google/OpenAI/buildwithclaude skill-creators, run via a fan-out Workflow) and a 9-unit backlog (SKILL-1…9) capturing the adoptable deltas: validator numeric budgets (desc ≤1024 chars, no `<>`, name regex, body line cap), explicit read-triggers on reference links, third-person + 3–5 enumerated trigger descriptions, script "agentic ergonomics" (truncated/traceback-free stdout), contamination-aware subagent forward-tests (the behavioral coverage gap), reference TOCs/grep hints, SKILL_STANDARD content-placement rules, anti-clutter sweep, tooling-fallback notes. The survey validated that the bundle's router+reference progressive disclosure, trigger descriptions, and deterministic scripts are already the industry consensus. Findings recorded only — no skill changed.
- `docs/tasks/op-watch-hardening.md` — markdown backlog (WATCH-1…5) materialized via op-planner: the op-watch loop-hardening units surfaced by a dogfood simulation (tri-state verdict, charter+gate-as-units, natural-key ingest, token→status mapping, fan-out tier routing).

### Changed
- `op-audit` + `op-watch` — **audit stance: discovery seeds a charter, scoped serves one** (F-L). op-audit gains `--scope <project|charter>` + `--paths`: `project` = discovery (whole codebase, "find anything" — *seeds* a remediation charter); `charter` = scoped (audit only the diff/findings against the plan — *serves* a charter, no project-wide wandering); `verify` is inherently charter-scoped. `audit-workflow.js` honors `scope`/`paths` (bounds the auditors). op-watch wires it: **derive runs discovery only for `ledger-clean` charters**; a build charter uses `op-planner` + start-of-run `op-audit run --spec` (plan-audit) + per-unit preflight — **never discovery in a build loop** (that's the focus-loss the charter prevents). The verify dispatch is now deterministic-first: `accept.mjs` always, then a **scoped semantic `op-audit verify --scope charter`** for HIGH-risk units / at epic closure. This makes audit + preflight the charter-scoped bookends of a unit and keeps the distraction-prone discovery audit out of the loop.

### Added
- `op-watch/scripts/accept.mjs` — **acceptance gate** (deterministic OUTPUT control at verify/closure, F-I-v2): a resolved unit must have its claimed `files` actually on disk AND keep them within the declared stack (via `scripts/lib/stack-check.mjs`); `--fix` demotes failures (missing→`in_progress`, stack-deviation→`deferred` pending a Decision) so `verdict.mjs` reflects reality instead of trusting status. Also: **op-watch's build dispatch now runs `op-preflight` first** (it never did — the loop went straight to "implement it", which is why preflight was never consulted), and the verify dispatch runs the accept gate. Caught on the "clean" kimi meta-server run what the status-trusting verdict missed: a **false-done** (story 1.2, 2/6 files) and a **silent stack deviation** (story 1.5 wrote Python tests in a Go repo). Smoke `op-watch-accept-smoke.mjs` 6/6; wired into the driver alongside `validate-state --fix`.
- `op-watch/scripts/{mark,verdict}.mjs` + SKILL — **no-progress no longer false-trips on build-before-verify; `done_condition` derivation made deterministic** (F-S/F-T). Surfaced dogfooding the slimmed skills on kimi: a fresh build campaign built all 4 stories (`open→built`) without yet verifying any, and the no-progress breaker tripped a premature CLEAN-HANDOFF — because `built ⊂ active`, so the active/resolved COUNTS stayed flat and the real `open→built` work was invisible. Fix (F-S): `mark.mjs` records a per-unit status-rank `score` (open 0 · in_progress 1 · fixed/built 2 · terminal 3) in each `history` entry; `verdict.mjs` trips no-progress only when resolved didn't rise **AND** active didn't fall **AND** score didn't rise (backward-compatible — pre-F-S states without `score` keep the count-only test). F-T: op-watch derive now sets `done_condition` deterministically (build → `epic-closure-gate` when an Epic Closure Gate is declared, else `units-verified`; remediation → `ledger-clean`) and never leaves it unset, and gate-as-units seeds only when `done_condition` is `epic-closure-gate`; dispatch now prioritizes **verifying a `built`/`fixed` unit before starting a new build** (verify-as-you-go keeps `resolved` rising). Smokes: verdict 20→22/22 (rising-score CONTINUE + flat-score HANDOFF), mark 9/9. `validate:dist` 19/0/0.
- `scripts/lib/fan-out-lane.mjs` + `op-audit`/`op-docup` engines + skills — **fan-out tier routing read `process.env` inside the Workflow sandbox, which has no `process`** (F-R). The fan-out engines run as Workflow scripts (sandbox: no Node globals — `process` is undefined), yet the canonical `LANE_MODEL_EMBED` and every engine that copied it (`audit-workflow.js`, `sweep-workflow.js`) read `process.env.OP_FANOUT_*` → hard crash `process is not defined` the moment `laneModel` evaluated (the op-audit run failed exactly here). A `typeof process` guard only masks it: in the sandbox `typeof process` is always `'undefined'`, pinning lanes to defaults forever and silently ignoring `OP_FANOUT_*`. Real fix: tier ids now flow through **`args`** (aliased `A`) — `LANE_MODEL_EMBED` = `(kind === 'write' ? (A.capableModel || undefined) : (A.cheapModel || 'haiku'))`; the SKILL (which runs in-session where env exists) resolves `OP_FANOUT_*` via `laneModels(process.env)` and forwards `{cheapModel, capableModel}` in the Workflow `args`. Engine defaults (`haiku`/inherit) keep lanes working with no args. Drift-guard `fan-out-lane-smoke.mjs` 13/13 now asserts the args-based embed; both engines `node --check` clean; zero `process.env` left in sandbox code.
- `op-watch/scripts/accept.mjs` + `verdict.mjs` — **the undeclared-stack skip no longer silently passes** (F-Q). Previously, when `accept` could not determine a declared stack (no `--declared` arg AND `Language(s)=TODO`/missing), it silently returned no stack deviations — the guard was off with no signal, so a polyglot spec could ship an unrequested language past it. Now: (1) `accept` **auto-detects** the stack from build manifests (`go.mod`→go, `package.json`→js, `Cargo.toml`→rust, `pyproject.toml`/`requirements.txt`→python, `pom.xml`/`build.gradle`→java, `*.csproj`→csharp, …) when the profile is unfilled — a single unambiguous signal enables the real check (labeled `[auto-detected]`); (2) if the stack is still undeterminable AND resolved units ship code files, `accept` emits a loud `ACCEPT-FAIL [undeclared-stack]` and records `gate_stack_undeclared` on the state (no per-unit demotion — over-broad); (3) `verdict.mjs` treats `gate_stack_undeclared` (and, newly, `gate_build`) as an **open campaign gate that blocks CLEAN-DONE** — so a project cannot silently reach DONE with the deviation guard off. Smoke: `op-watch-accept-smoke.mjs` 17→21/21, `op-watch-verdict-smoke.mjs` +2 (gate-block).
- `templates/AGENTS.md` + `templates/docs/HANDBOOK.md` + `op-preflight` + `op-planner` — **make the declared stack obvious; bar a model from self-authorizing a language** (F-P). Root cause of models drifting to a language nobody asked for (e.g. Python in a Go repo): an unfilled Stack Profile `Language(s) | TODO` *silently disables* the stack guard, the HANDBOOK never states the language, and a polyglot spec then fills the vacuum. Fixes: AGENTS.md template warns loudly that `TODO` disables the guard and reworded the row to `REQUIRED, e.g. go`; HANDBOOK template + project HANDBOOK gain a `## Stack` section stating the primary language up front; `op-preflight` adds an **undeclared-stack gate** (missing/`TODO` `Language(s)` + creating code → `WATCH`, declare first) and requires a stack-deviation `### Decision` to be **human-accepted** (a model drafts `pending` and hands off, never flips its own deviation to `accepted`); `op-planner` detects the real language from `go.mod`/etc. when the profile is unfilled and plans every story in it. Composes with F-O (which honors a human-`accepted` stack ADR).
- `op-watch/scripts/accept.mjs` — **honors an ACCEPTED stack Decision** (F-O): the gate now reads `docs/decisions/decisions.yaml` and treats a stack deviation whose files are covered by a `status: accepted` `category: stack` ADR as decided (passes); a `pending`/undecided deviation still demotes. Without this a deliberately-recorded second language (e.g. Python integration tests beside Go) was re-flagged every step → `deferred` forever → CLEAN-DONE unreachable. Surfaced by a deepseek-v4-pro full-cycle run that recorded + accepted exactly such a Decision (ADR-002). Also fixed the eval driver's focus classifier to treat `docs/decisions/` writes as on-charter (recording a gate-required Decision is not a distraction). Smoke `op-watch-accept-smoke.mjs` 15→17/17.
- `op-watch` + `op-preflight` — **test plan before start (proactive language gate) + auto-drafted pending Decision on deviation** (F-N). The reactive accept gate caught the Python-tests-in-a-Go-repo deviation *after* the model authored a throwaway suite; this decides the test language *up front*. (1) op-watch derive freezes `charter.test_language` (= primary declared Stack-Profile language) so the test stack is a campaign-level decision, not a per-story improvisation; (2) op-preflight's test-touching route now **requires a `Test Plan` (`{language, framework, isolation, run-cmd}`) before any test code** and stack-checks the planned language vs the charter/declared stack — a non-declared test language is a plan-time `### Decision` (`WATCH`), decided before authoring; (3) `accept.mjs --decisions <decisions.yaml>` drafts a `status: pending` ADR for each stack deviation (idempotent per unit, op-decision-memory remains the canonical writer that confirms it) so the handoff carries a ready decision record, not just a note. Proven on the live kimi deviation: `ADR-001` drafted with the deviation, options (rewrite in go / declare python + isolate), and affected files; the unit note references `[draft ADR-001 pending]`. Smoke `op-watch-accept-smoke.mjs` 12→15/15 (draft append + note tag + idempotent re-run). Wired into the driver + SKILL verify gate.
- `op-watch/scripts/accept.mjs` — **manifest-independent + functional hardening** (F-M): the base check trusted the model's self-reported `files[]` (a model that omits a deviant file, or ships a hollow stub, slipped past). Three opt-in modes close the gap — `--scan` walks the directories the resolved units actually wrote into and stack-checks *every* code file there (incl. **gitignored** siblings the manifest omitted; scoped so it never false-positives on unrelated repo files), `--min-bytes N` flags a claimed file that exists but has < N bytes of substantive (non-blank/non-comment) content (existence ≠ substance), and `--build "<cmd>"` adds a compile-level functional gate at closure (`gate_build`). A candidate experiment (`scripts/eval/accept-candidates-exp.mjs`) proved `--git`-diff truth catches *neither* hole when the deviant file is gitignored — `--scan` is the robust file-truth source. Base behavior byte-stable (opt-in flags); SKILL verify-step + driver now pass `--scan --min-bytes 40 --fix`. Smoke `op-watch-accept-smoke.mjs` 6→12/12.
- `templates/docs/feedback/test-infra.md` + `templates/docs/testing/{testing-rules,e2e-maintenance-guide}.md` — **testing/feedback harvest** (generalized from a downstream project): a reusable test-infrastructure feedback file (`TI1-TI10`: per-run isolation, template-DB reuse, parallel-safety, no-bare-mock-for-typed-dep, local-E2E-only, data-via-API, one-source personas, contract selectors, live-owned-services-mock-only-external, project-correct runner) + canonical testing-rules + an E2E maintenance guide. `feedback/index.md` gains a `test-infra.md` route row + a tests/E2E quick-selector; `feedback/process.md` gains the **Preflight Content Standard** (Scope/Invariants/Risk/Validation), authoring rules (one-file-per-topic, stable IDs, realistic BAD/GOOD, remove-obsolete), and blocker criteria. `op-preflight/SKILL.md` now **routes test-touching changes** to `test-infra.md` + the testing docs and treats a test-infra violation (shared state, hardcoded seed IDs, fragile selectors, mocking owned services, weakened assertions) as a gate, not a nuance. Atour-specific stack/incidents stripped to `<...>` placeholders.
- `scripts/lib/stack-check.mjs` — **stack-deviation guard** (GP1 declared-wins): flags files whose language is not in the project's declared Stack Profile language(s); config/docs/shell are stack-neutral. Wired into `principles.md` GP1, `op-planner` (plan-time flag → record a Decision + isolate the story in a separate dir), and `op-preflight` (a non-declared language scores `WATCH`, not `PASS`, until the decision is logged). Motivated by the meta-server flash run: the model faithfully built the spec's reference **Python FastAPI adapter inside a Go repo** but never flagged the Go↔Python deviation — a spec's *illustrative* code is not a mandate to add a language. Smoke: `stack-check-smoke.mjs` 7/7 (the meta-server case → `adapter.py` flagged, `launch.sh`/`.toml`/`.md` neutral).
- `op-watch/scripts/init.mjs` + `op-watch/scripts/validate-state.mjs` — **state-integrity guards** (F-H, from the meta-server full-cycle flash run): a cheap model that hand-wrote the campaign state corrupted it twice — a missing brace in the *derive* step (stalled the campaign) and a *duplicate unit id* (silently inflated the active count so the loop couldn't converge). `init.mjs` writes the initial state from a charter + units array atomically (dedups ids, valid JSON) so derive is never hand-authored; `validate-state.mjs --fix` repairs a duplicate id / rejects unparseable state as a per-step gate. `verdict.mjs` now also **detects duplicate unit ids deterministically** (`breaker:dup-ids`, refuses CLEAN-DONE) so the loop catches corruption even if the validator is skipped — turning the silent meta-server stall into a CLEAN-HANDOFF + auto-repair. SKILL.md derive/step-4 mandate the scripts. Smoke: `op-watch-state-integrity-smoke.mjs` 8/8; verdict smoke +2 dup-id cases (14→16).
- `op-watch/scripts/mark.mjs` — **step-4 mechanization** (WATCH-4, F-G): records a unit transition atomically (set status · `attempts++` · append a `history` entry with recomputed active/resolved counts · bump `step` · temp+rename), so the counters `verdict.mjs` relies on are maintained by a script, not agent prose. Wired into SKILL.md step 4; smoke `op-watch-mark-smoke.mjs` 9/9. Proven by an end-to-end loop run on a real testbed folder (ingest real findings → fix files → verify with teeth → `CLEAN-DONE`), by ingesting crateon-dev's real 52-finding op-audit ledger, and by a **fully autonomous** end-to-end run (`scripts/eval/run-op-watch-e2e.mjs`) where each of deepseek-v4-flash, kimi-highspeed, mimo-v2.5-pro edited the real files itself and drove the campaign to `CLEAN-DONE` — **3/3**, graded on disk (secret removed, `clamp(-5,0,10)===0` executed).
- `scripts/lib/fan-out-lane.mjs` — **shared fan-out tier policy + self-escalation** (WATCH-5): one canonical, tested definition of how a Workflow fan-out lane picks a model tier (read-only/audit/propose → cheap `OP_FANOUT_CHEAP_MODEL` default `haiku`; write/synthesis → capable `OP_FANOUT_CAPABLE_MODEL` default inherit-session) and self-escalates a cheap lane ONCE to capable on under-scope. `op-docup/sweep-workflow.js` and `op-audit/audit-workflow.js` embed + cite it (marker `fan-out-lane:v1`, drift-guarded by `scripts/eval/fan-out-lane-smoke.mjs` 13/13); they stay separate files (Workflow sandbox can't import siblings; doc-sync vs multi-lane audit are distinct domains). op-watch dispatch stays single-tier. Control pass: drift guard hardened to assert the embedded tier expression (`LANE_MODEL_EMBED`), not just the marker comment. op-audit's self-escalation is **aligned** to the lib contract (F-C, closed): `FINDINGS_SCHEMA` gained a required `escalate` boolean, the auditor prompt requests it on under-scope, and the lane re-runs once on the capable model when `r.escalate === true` (or a null/dead result) — drift-guarded. ingest auto-id counter fixed (`u-1,u-2,u-3`).
- `scripts/lib/finding-key.mjs` + `op-watch/scripts/ingest.mjs` — **natural-key findings ingest** (WATCH-3): promotes the content-derived finding identity (domain + locus [+ defect-class], line/skill-independent) from the eval harness to a shippable shared lib, and adds an ingest helper so op-watch derive/re-baseline merge an audit/drift/debt ledger into campaign units **without duplicating** a defect two skills reported independently — and re-baseline matches existing units by key instead of re-opening a just-closed one. Gated on the Part B proof (`id-dedup-check` real+fixture PASS; `id-stability-replay` key invariant across renames + churn). Smoke: `op-watch-ingest-smoke.mjs` (3/3). `lib/finding-key.mjs` added to `publicScripts`.

### Changed
- `op-watch/SKILL.md` + `op-decision-memory/SKILL.md` — **dispatch token→status mapping + idempotent re-entry** (WATCH-4): op-watch now maps each delegated sub-skill's completion token to a unit status deterministically (`DONE→fixed`, `BLOCKED→blocked`, `DONE_WITH_CONCERNS→fixed+note`, `NEEDS_CONTEXT→blocked` **terminal-after-1** so a context-starved step can't loop), increments per-unit `attempts`, and reconciles status from the repo on resume. op-decision-memory's `--apply` append is now **idempotent by identity key** (category + normalized decision/affects, not the `ADR-NNN` id) so a crashed-then-resumed op-watch step can't double-append an ADR.
- `op-watch/SKILL.md` — **charter schema + gate-as-units + branch guard** (WATCH-2): the derive step now writes a **frozen** `charter{intent, done_condition, anchor, spec_system, branch}` (read, never re-derived → idempotent across compaction); when `done_condition` is an epic gate it seeds the Epic Closure Gate steps as their own units (`gate:drift/docup/drift2/decision/checklist`) so findings-empty ≠ epic-done; and a default-branch (`main`/`master`) + due write sub-step is `deferred` with a reason instead of writing to main hands-off (GP4).
- `op-watch/scripts/verdict.mjs` — **tri-state verdict + terminal breakers** (WATCH-1): replaces the binary CLEAN/CONTINUE with `CLEAN-DONE` (done) / `CLEAN-HANDOFF` (loop stops, **not** done) / `CONTINUE`. The breakers are now read from state by the script, not trusted to agent prose: `budget.spent ≥ total`, any active unit with `attempts ≥ 3`, and no net progress over K `history[]` steps each force `CLEAN-HANDOFF` — so the stop decision is deterministic from state, not the model's say-so (the budget breaker previously only annotated and never terminated). An absolute **step-cap** (F-D, closed) trips `CLEAN-HANDOFF` once a campaign with active work passes `s.max_steps` (default `max(20, units×5)`) — since `mark.mjs` bumps `step`/`history` by script, this guarantees termination of a productive-but-non-converging loop independent of the model; the documented goal-driver invocation cap is the outer backstop for a spin that never records a step. The no-progress breaker treats a flat window as a stall **only** when every active unit has been tried (`attempts>0`) — units pending dispatch (e.g. gate steps, `attempts===0`) are forward work, not a stall (F-E). And `fixed`/`built` (await-verify) now count as **active** so a lone fixed unit does not hand off before its verify step runs (F-F). Both bugs were found by dogfooding op-watch as the dispatcher on fast models (deepseek-v4-flash, kimi-highspeed, mimo-v2.5-pro): **14/15** on the WATCH-2/4 dispatcher contracts via `scripts/eval/run-op-watch-step.mjs` (the one miss is a mimo model-adherence nuance on the severity-gate label, not a defect). Adds `units[].attempts` and `history[] {step,active,resolved}` to the state schema; `SKILL.md` invariants 3–5 + Status/Completion sections updated to match. **F-J** (found on north-mini-code-free, which wrote a non-contract `stories` schema with no `units`): CLEAN-DONE now requires `units.length > 0` — a state with zero recognized units (derive never populated it, or a wrong schema) is **not vacuously done** (was a false CLEAN-DONE — the exact "fooled into stopping early" failure). Smoke matrix: `scripts/eval/op-watch-verdict-smoke.mjs` (18/18, incl. F-D/F-E/F-F/F-J + dup-id cases).

### Added
- `templates/docs/principles.md` — shared cross-cutting conventions (GP1–GP9: declared-wins, checkpoint-first, git boundary, autonomy boundary, proof-targets-outcome, verify teeth, resumable state, completion protocol, brevity) stated once, referenced from `templates/AGENTS.md`. Skills log a line only to *deviate*, never to repeat — DRY-ing the conventions that op-* skills previously each restated (Part B harvest: shared principles include).

## [0.9.0] - 2026-06-20

### Added
- `op-watch` skill + `scripts/verdict.mjs` (Part C / rev-3) — drive a remediation or build **campaign** to completion one bounded step per invocation under a `goal` loop: `goal op-watch "<intent>"`. op-watch is a **thin dispatcher** (delegates to op-audit/op-debug/op-planner, owns no analysis); each step reads `docs/.work/<slug>.json`, dispatches the right sub-step (derive/build/remediate/verify/re-baseline), persists state atomically, and emits a **deterministic CLEAN/CONTINUE verdict computed by `verdict.mjs` over the state** (never model-asserted — so a goal loop can't be fooled into stopping early). Three status buckets (active / terminal-resolved / terminal-handed-to-human); CLEAN = no active units (deferred/blocked surfaced). Encodes the eight "nothing-unresolved" invariants (status totality, 3-strikes, non-convergence, budget, severity-defer, verify-teeth, surfaced deferred/blocked) + two boundary gates (derive coverage, fake-clean) + a resumability contract (atomic writes, status-from-repo, persisted counters/charter, re-armable goal). Registered in the operational-development-cycle marketplace group; `docs/.work/` gitignored. (See `docs/reports/2026-06-20-op-spec-system-and-ultrapack-harvest-plan.md` rev 3.)

## [0.8.0] - 2026-06-20

### Added
- `op-docup/scripts/sweep-driver.mjs` — **boundary-check** (Part B harvest): after each per-segment call, assert the introduced doc changes stay inside the segment's own `docs/<track>/` (merge-owned `INDEX.md`/taxonomy excepted); a segment that wrote outside its track is recorded (`boundary_violation`) and logged. Mechanizes the parallel-safety we previously only asserted.
- `op-debug` skill (Part B harvest) — root-cause-first debugging: reproduce → trace the bad value back to its source → fix at the source (Iron law: no symptom patches — no `try/except:pass`, `.get(default)`, `sleep(N)`) → anti-whack-a-mole sweep (name the bug pattern, grep the repo for the same shape, fix in one change) → bounded validation. **Three-strikes circuit-breaker:** after 3 failed fixes, stop and escalate as an architecture problem. Default investigates + proposes one fix; `--apply` writes it. Fills the one missing lifecycle stage (debug). Registered in the operational-development-cycle marketplace group.
- Autonomy-boundary rule in `templates/AGENTS.md` (Part B harvest): running hands-off (headless / `--apply` / `--dangerously-skip-permissions` / goal-loop) **never invents a required-but-unspecified value** (no guessed `timeout=30` etc. — use the stated/prior value or defer), follows a **reversibility ladder** (rename-before-delete · add-before-replace · stash-not-reset · branch-never-main · stop-not-destroy; if unsure it's reversible, defer), and splits hands-off checkpoints into `### Decisions` vs `### Deferred (needs user input)` (deferred is never silently dropped). Complements the git-boundary rule.

### Changed
- `op-audit` / `op-drift-check` — verify teeth (Part B harvest): an op-audit `fixed`/`verified` verdict must cite the `file:line` re-read in this run as evidence ("linter passed" / "agent said done" / "should work" are not evidence and keep the finding open), and a finding moves to `wont_fix` only with a scope-exclusion or new-fact reason (no silent demotion to "future work"). op-drift-check gains a default refute stance — "assume the doc claim is stale; prove it against the code"; an unverifiable claim is at least a WARNING. Exact-string protocols byte-identical.
- `op-preflight` — proxy-gap in the Proof path (Part B harvest): the proof must target the User Spec's outcome, not a stand-in; when only a proxy is runnable (local smoke vs real-scale/remote/user-environment outcome), name the gap and score that item `WATCH`, not `PASS`. Exact-string protocol untouched.
- `op-audit` — task export + greenfield awareness (Part A): `tasks` exports findings into the project's **task/spec system** (BMAD story · spec-kit `tasks.md` row · bead · `gh issue` · markdown), detected per `docs/spec-systems.md` or forced via `--system`; backend-tagged `task_ref` prevents double-export and a fixed finding closes its unit (beads is now one recipe, not the privileged default). New `run --spec` lane audits the **planned backlog** (missing/untestable ACs, duplicates, orphans, plan-vs-code drift, contradictions) into the ledger under domain `spec`. Guided-help on a greenfield repo routes to `run --spec` or to `op-planner`/`op-story-finder` instead of dead-ending on "no code". Merge-safe; completion-status byte-identical.
- `op-planner` / `op-story-finder` — de-BMAD'd to read the project's task/spec system (Part A): planner determines the system (Stack Profile / `docs/spec-systems.md`) and materializes the unit in its format (BMAD story+epic-table · spec-kit tasks.md row · beads `bd create` · markdown entry); story-finder searches the detected system's unit files. BMAD stays the built-in default illustration. Merge-safe; completion-status sections byte-identical.
- Spec/task-system awareness (Part A): `templates/AGENTS.md` Stack Profile gains `Task / spec system` + `Spec unit format` rows, and a new `templates/docs/spec-systems.md` defines detection (declared-wins → spec-kit → BMAD → beads → markdown → issues → none) + a uniform adapter contract (locate/find/create/export/list) + illustrative per-system recipes (open set). Planning/audit skills read this instead of hardcoding BMAD/beads. Scaffold-only template; merge into existing projects manually. (See `docs/reports/2026-06-20-op-spec-system-and-ultrapack-harvest-plan.md`.)

## [0.7.0] - 2026-06-19

### Added
- `op-docup` sweep **driver loop** — new `skills/op-docup/scripts/sweep-driver.mjs`, the hard cross-model completion mechanism for stall-prone / weak models. It re-invokes the model **one segment per fresh invocation** over the resumable plan-file (host command supplied via `--cmd` with `{ROOT}`/`{PROMPT}`/`{TO}` placeholders — host-agnostic), runs `segment.js` for the deterministic scope-freeze first, is **robust to status-lag** (marks a segment done from the docs it actually wrote, not the model's self-report), and retries-then-advances on a stall (never an infinite loop) before a bounded merge call. Makes `--batch` a hard guarantee instead of advisory. Empirically: regular kimi — which never finished a 20- or 6-commit sweep in a single invocation (stalled) — completed all 5 segments + merge across 6 bounded driver calls (`status: complete`). Repo/ops mechanism; the skill body just points to it. Completion-status protocol byte-identical.

## [0.6.0] - 2026-06-19

### Added
- `op-docup` sweep **deterministic segmentation** (rev-3) — new `skills/op-docup/scripts/segment.js` classifies changed files to documentation tracks by a FIXED precedence (declared `docs/.docup/trackmap.json` → reverse-index from existing stories'/epics' "Files Modified" citations → persistent rubric index `docs/.docup/rubrics.json` → existing `docs/<track>/` name → structural default for source code; config/scaffold skipped). The segment set is now **identical across runs and models** — fixes the model-dependent-segmentation drift recorded in the benchmark (deepseek 5 / kimi 2 / Opus 3 → one deterministic decomposition). Metadata-only (paths/subjects/`--stat`), so input stays cheap and corpus-independent; the rubric index self-feeds from op-docup's own "Files Modified" writes and converges; `--batch=N` (or Stack Profile) sub-batches a heavy segment for weak models (pass count tracks segment count, not commit count). Phase-1 scope-freeze runs the segmenter, with a prose longest-prefix fallback when node is unavailable. Completion-status protocol byte-identical. **Replace the Sweep section + add `scripts/segment.js`; protocol unchanged.**

### Added (earlier in this cycle)
- `docs/reports/2026-06-19-op-docup-sweep-benchmark.md` — A/B benchmark ledger comparing the new sweep `op-docup` (0.4.0 Tier-2, 0.5.0 Tier-1) against the old single-context version. Records the validated runs (deepseek Tier-2: bounded, 0 provider errors vs the old 524k-token + 503-breaker baseline; kimi-highspeed Tier-2: completed end-to-end in ~9 min with a real index-reconciling merge, 0 errors; kimi-regular Tier-2: impractically slow — 2 docs in ~40 min, no segment completed, stalled, confirming highspeed as the sweep driver; Opus Tier-1: 3 parallel segments, 113 s, ~44k tokens/segment, complete), the model-dependent-segmentation caveat (variance even within the kimi family), and the recipe for a fully controlled same-model A/B.

## [0.5.0] - 2026-06-19

### Added
- `op-docup` sweep **Tier-1 subagent fan-out** (`skills/op-docup/scripts/sweep-workflow.js`) — the accelerator half of the sweep mode. Where the host exposes the Workflow tool, the frozen segments are dispatched one bounded doc-sync agent per documentation track (each writes only its own `docs/<track>/` and returns a compact summary), for true within-run context offload + parallelism; falls back to sequential `Task` agents, then to the Tier-2 sequential pass. Parallel-safe without worktree isolation (distinct track dirs); the skill still owns the single cross-segment merge and the durable plan-file, so Tier-1 and Tier-2 are interchangeable and resumable. `op-docup` frontmatter gains `allowed-tools` (adds `Workflow`, `Task`). Completion-status protocol untouched. **Replace the Workflow/Sweep sections + add `scripts/sweep-workflow.js`; protocol section byte-identical.**

## [0.4.0] - 2026-06-18

### Added
- `op-docup` **sweep mode** (`--sweep`, also auto-detected on large change sets) — a resumable, segment-bounded doc-sync pass. Scope-freeze (names-only) writes a durable plan-file (`docs/.docup/sweep-state.json`); a sequential per-segment pass bounds context to one track at a time and persists progress after each segment, so a timeout/interruption resumes instead of restarting; a final merge reconciles indexes once. Portable (no subagent tool required); subagent fan-out is an optional accelerator. Default single-pass behavior unchanged for small repos (no regression). Completion-status protocol untouched — a partial pass uses the existing `DONE_WITH_CONCERNS` token with the remaining count carried in the plan-file. Mechanics in `skills/op-docup/references/sweep.md`. **Replace the Workflow/Usage/description sections; protocol section is byte-identical.**

## [0.3.0] - 2026-06-18

### Added
- `op-audit` skill — full-codebase audit that writes a report plus a durable `docs/audit/audit-findings.json` ledger with content-addressed finding ids and status preservation across re-runs. Registered in the `operational-development-cycle` marketplace plugin and shipped to dist.
- Stage-replay verification harness (`scripts/eval/run-stage-replay.mjs`, `replay-stages.json`, `score-stage-replay.mjs`) — walks a repo's monthly milestones forward, runs the op-* lifecycle per stage (clean-rebuild, carry agent docs forward), and scores operability + cross-skill coordination. Supports `--client codex|claude|codex-native`. Per-step authorship is captured as a delta and severity/finder metrics are counted over a cleaned report view (drops echoed skill-body rubric and model-authored helper code), so transcript verbosity no longer inflates the counts. Repo-only (not shipped to dist).

### Changed
- Git boundary: write skills (`op-planner`, `op-docup`, `op-feedback-harvest`, `op-decision-memory`, `op-preflight`, `op-audit`) and `templates/AGENTS.md` now forbid `git add`/`commit`/`reset` — only `baby-commit`/`dry-commit` stage/commit, on explicit request. Closes a hazard observed in the stage-replay experiment (an agent with `--dangerously-skip-permissions` auto-committed in a detached worktree and stranded later steps). Protocol/Completion-status sections untouched.
- `scripts/eval/replay-stages.json` — recorded the recommended driver: `--client claude --key KIMI_API --model kimi-k2.7-code-highspeed` + `--timeout-scale 4` (fastest reliable; ~2–6× over regular kimi; deepseek docup trips DeepSeek's circuit breaker; kimi-for-coding 403s non-claude clients).
- Added the stage-replay experiment write-up (`docs/reports/2026-06-18-op-skills-stage-replay-experiment.md`) and the op-docup-sweep integration plan (`docs/reports/2026-06-18-op-docup-sweep-plan.md`).
- `op-preflight` — template-only scaffold handling: when `docs/feedback/index.md` routes no project-specific rules, mine declared rule sources (`AGENTS.md`, `CLAUDE.md` critical rules) as ad-hoc rules and push toward `op-feedback-harvest --apply` (merge-safe: one workflow bullet).
- `op-feedback-harvest` — declared agent-contract rules (AGENTS.md, CLAUDE.md critical rules, CHANGELOG lessons) are first-class harvest candidates alongside commits (merge-safe).
- `INSTALL.md` — shadow-check one-liner for stale pre-`op-*` copies; migration table from pre-`op-*` names; layout-change/pycache update guidance; template-only feedback window note; new "Adapting to your stack" section (declare conventions, never edit skill copies).
- De-bias pass (all merge-safe one-liners; protocol contracts untouched): `op-drift-check` manifest list opened (Maven/.NET/Composer/...) + archetype claim surfaces; `op-debt-scan` size thresholds marked as project-overridable defaults; `op-tour` archetype list extended (GUI/ML/embedded/game) + open manifest set; `op-planner` layer rule generalized + track examples marked illustrative; `op-docup` track examples marked illustrative; `op-story-finder` follows declared docs layout, "no story docs" = DONE not BLOCKED; `baby-commit`/`dry-commit` 300 lines = default, project budget overrides; `op-feedback-harvest` file mapping marked illustrative; `upstream-harvest` tests in upstream's language + archetype categories; `project-doc-kit` archetype contract tables + graceful DOCX skip; `agents/test-runner.md` rule zero (declared test command wins; go/cargo/dotnet/mvn added; local non-Docker fallback).
- `templates/AGENTS.md` — new Stack Profile section (declared language/archetype/commands/layout/budgets that skills read before defaults); `templates/docs/feedback/baseline.md` — archetype-mapping note.
- Removed hardcoded external sprint-tracker paths from `op-tour` and `op-planner`. Sprint/board update is now optional — only when the project already tracks such a file (path declared in AGENTS.md/HANDBOOK); the bundle neither owns nor scaffolds it (merge-safe).
- Knowledge-placement seam (closes a feedback↔architecture drift risk): `op-feedback-harvest` now decides rule ownership before mapping — a candidate that restates an invariant owned by a source-of-truth doc (architecture/design/HANDBOOK/baseline) becomes pointer-form, not a copy; dedup widened to those docs; rule shape gains an `Origin:` provenance line (incident / design-invariant / imported-baseline). `op-drift-check` gains a doc-vs-doc duplication check (a feedback rule restating an owned invariant → INFO). All merge-safe; protocol contracts untouched.
- Epic Closure Gate is now an **ordered** sequence in `templates/AGENTS.md` (and HANDBOOK cadence): `op-drift-check` → `op-docup --epic --apply` → re-check until clean → `op-decision-memory` last → closure checklist. Resolves the one real cross-skill ordering gap found in the system audit (drift-check detects what docup fixes; recording decisions before docs settle risks a stale rationale).
- Added repo-only verification harnesses under `scripts/eval/` (not shipped to dist): `finding-key.mjs` (shared natural-key reference impl), `id-dedup-check.mjs`, `id-stability-replay.mjs`, `ledger-history-retro.mjs`. Empirically validated the shared finding-ID design on a real 1178-commit repo: viable as `domain | line-stripped-paths | controlled-anchor` with a `--follow`/rename-alias step; free-text predicates do NOT converge cross-skill (controlled defect-class/symbol token required).

## [0.2.1] - 2026-06-12

### Changed
- ALL 16 skills — completion-status protocol hardened: the last line must start with an exact status token; imperative wording. Replace-safe everywhere.
- `op-preflight` — exact-string protocol for `PR/security risk:` line and the four verdict strings; project-declared mechanical gates belong in the Proof path. **Replace, don't merge.**
- `op-debt-scan` — literal `priority-score: N (...)` format required per MEDIUM/HIGH finding; file-budget baseline read as seeded debt with ratchet semantics. **Replace, don't merge.**
- `op-drift-check` — CURRENT/TARGET doc-marking convention; `Known Drift (audited <date>)` stamp; dead-public-surface check. **Replace, don't merge.**
- `op-docup` — epic-closure mode `--epic=<id>`; restore-or-delete rule for vanished documented features. **Replace, don't merge.**
- `op-planner` — closure-checklist injection into materialized epics when the project ships a template. Replace-safe.
- `op-decision-memory` — Epic Closure Gate named as a standard trigger. Replace-safe.
- `templates/AGENTS.md`, `templates/docs/HANDBOOK.md` — new Epic Closure Gate and Review Cadence sections (scaffold templates; merge into existing projects manually).
- `graphify-loop` — internal layout moved scripts into `scripts/`. **Replace the folder wholesale.**

### Added
- `search-adapter`, `service-checker` skills.
- `scripts/eval/` — discovery A/B harness and op-cycle lifecycle harness (repo-only, not published to hosts).

## [0.2.0] - 2026-06-10

### Changed
- Skill standard rework: `op-*` renames (`planner` -> `op-planner`, `memory` -> `op-decision-memory`, ...), checkpoint-first safety contracts (`--apply` gates), trigger-phrase descriptions, `allowed-tools`/`safety-class` frontmatter, heavy bodies trimmed into `references/`. **Replace all pre-`op-*` copies.**
